# 암호화폐 포트폴리오 시뮬레이터

LSTM 기반 백테스팅 및 최적 매매 타이밍 분석 웹 애플리케이션

## 기능

- **백테스팅 시뮬레이션**: 과거 데이터를 기반으로 매매 전략의 성과를 분석
- **기술적 지표 분석**: RSI, MACD, 볼린저 밴드, 이동평균선 등 다양한 지표 제공
- **최적 매매 타이밍 탐지**: RSI와 MACD를 기반으로 한 자동 매수/매도 신호 생성
- **성과 지표**: 수익률, 승률, MDD, Sharpe Ratio 등 상세한 성과 분석
- **인터랙티브 차트**: 가격 차트, RSI, MACD 차트를 탭으로 전환하여 확인 가능
- **시각적 차트**: 그라데이션 영역, 변동세 표시, 커스텀 툴팁

## 기술 스택

### Backend
- Python 3.14
- Flask (REST API)
- Pandas, NumPy (데이터 처리)
- scikit-learn (전처리)
- Matplotlib (차트 생성)

### Frontend
- React
- Recharts (차트 라이브러리)
- Axios (HTTP 클라이언트)

## 빠른 시작

### 한 번에 실행하기 (권장)

```bash
cd /Users/jastes/Desktop/hackaton
./run.sh
```

이 스크립트가 자동으로 Flask 서버와 React 앱을 시작합니다.

### 수동 실행 방법

**터미널 1 - Flask 서버:**
```bash
cd /Users/jastes/Desktop/hackaton
source venv/bin/activate
python app.py
```

**터미널 2 - React 앱:**
```bash
cd /Users/jastes/Desktop/hackaton/frontend
npm start
```

### 접속 주소

- 🌐 **웹 애플리케이션**: http://localhost:3000
- 🔌 **API 서버**: http://localhost:5001/api
- 📊 **API Health Check**: http://localhost:5001/api/health
- 📋 **마켓 목록**: http://localhost:5001/api/markets

## 설치

### Python 백엔드

```bash
# 가상환경 활성화
source venv/bin/activate  # macOS/Linux
# 또는
venv\Scripts\activate  # Windows

# 필요한 패키지 설치
pip install -r requirements.txt
```

### React 프론트엔드

```bash
cd frontend
npm install
```

## 사용 방법

1. **백테스팅 설정**
   - 암호화폐 선택 (비트코인, 이더리움 등)
   - 분석 기간 설정 (100-1000일)
   - 초기 자본금 입력
   - 실제 API 데이터 사용 여부 선택 (체크 해제 시 샘플 데이터 사용)

2. **백테스팅 실행**
   - "백테스팅 실행" 버튼 클릭
   - 결과가 자동으로 표시됩니다

3. **결과 확인**
   - **성과 지표**: 총 수익률, Buy & Hold 수익률, 승률, MDD, Sharpe Ratio 등
   - **차트 분석**: 가격 차트, RSI, MACD 차트를 탭으로 전환하여 확인
   - **거래 내역**: 모든 매수/매도 거래 내역을 테이블로 확인

## 매매 전략

### 매수 신호
- RSI < 30 (과매도) 그리고 MACD 골든 크로스
- 볼린저 밴드 하단 돌파 후 반등

### 매도 신호
- RSI > 70 (과매수) 그리고 MACD 데드 크로스
- 볼린저 밴드 상단 도달

## API 엔드포인트

### `GET /api/health`
서버 상태 확인

### `GET /api/markets`
사용 가능한 암호화폐 마켓 목록 반환

### `POST /api/backtest`
백테스팅 실행

**Request Body:**
```json
{
  "market": "KRW-BTC",
  "days": 500,
  "initial_capital": 10000000,
  "use_api": false
}
```

## 문제 해결

### 실행 권한 오류
```bash
chmod +x run.sh
chmod +x start_server.sh
```

### 포트가 이미 사용 중인 경우
```bash
# 포트 5001 종료
lsof -ti:5001 | xargs kill -9

# 포트 3000 종료
lsof -ti:3000 | xargs kill -9
```

### npm 패키지 오류
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
```

### Python 패키지 오류
```bash
source venv/bin/activate
pip install -r requirements.txt
```

### macOS 포트 5000 문제
macOS에서 포트 5000은 AirPlay가 사용하므로, 이 프로젝트는 포트 5001을 사용합니다.

## 주의사항

- 이 도구는 교육 및 연구 목적으로 제작되었습니다
- 실제 투자 결정에 사용하기 전에 충분한 검증이 필요합니다
- 과거 성과가 미래 수익을 보장하지 않습니다
- 암호화폐 투자는 높은 리스크를 수반합니다

## 프로젝트 구조

```
hackaton/
├── app.py                 # Flask 서버 메인 파일
├── crypto_simulator.py    # 백테스팅 로직
├── requirements.txt       # Python 의존성
├── run.sh                 # 통합 실행 스크립트
├── start_server.sh        # Flask 서버만 실행
├── frontend/              # React 앱
│   ├── src/
│   │   ├── App.js
│   │   └── components/
│   └── package.json
└── README.md              # 이 파일
```

## 라이선스

MIT License
