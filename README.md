# 암호화폐 포트폴리오 & 백테스팅 시뮬레이터

React + FastAPI 기반의 암호화폐 포트폴리오 관리 및 백테스팅 시뮬레이터 웹 애플리케이션입니다.

## 프로젝트 구조ee

```
.
├── front/          # React 프론트엔드
│   ├── src/
│   │   ├── components/
│   │   │   ├── atoms/      # 기본 UI 컴포넌트
│   │   │   ├── molecules/  # 조합 컴포넌트
│   │   │   ├── organisms/  # 복잡한 섹션 컴포넌트
│   │   │   └── pages/      # 페이지 컴포넌트
│   │   └── types/
│   └── package.json
│
└── backend/        # FastAPI 백엔드
    ├── app_fastapi.py
    ├── crypto_simulator.py
    ├── requirements.txt
    └── README.md
```

## 주요 기능

### 프론트엔드
- 암호화폐 실시간 가격 대시보드
- 포트폴리오 관리
- Past ROI 분석 및 차트
- 백테스팅 시뮬레이터
- **매매 일지 (Trade Journal)** 📝
  - 매매 기록 CRUD (생성, 조회, 수정, 삭제)
  - 실시간 통계 및 수익률 계산
  - 매수/매도 내역 추적
  - 수수료 관리 및 메모 기능
- 반응형 디자인

### 백엔드
- LSTM 기반 가격 예측
- RSI, MACD, 볼린저 밴드 등 기술적 지표 분석
- 백테스팅 시뮬레이션
- 최적 매매 타이밍 분석
- 상승 확률 계산
- **SQLite 기반 매매 일지 데이터베이스**
- RESTful API 제공

## 설치 및 실행

### 1. 백엔드 실행

```bash
cd backend

# 의존성 설치
pip install -r requirements.txt

# 서버 실행 (방법 1)
python app_fastapi.py

# 서버 실행 (방법 2 - Windows)
start.bat

# 서버 실행 (방법 3 - Linux/Mac)
./start.sh
```

백엔드 서버가 http://localhost:5001 에서 실행됩니다.

### 2. 프론트엔드 실행

```bash
cd front

# 의존성 설치
npm install

# 개발 서버 실행
npm run dev
```

프론트엔드가 http://localhost:5173 에서 실행됩니다.

## API 문서

백엔드 서버 실행 후 다음 URL에서 자동 생성된 API 문서를 확인할 수 있습니다:
- Swagger UI: http://localhost:5001/docs
- ReDoc: http://localhost:5001/redoc

## 기술 스택

### Frontend
- React 18
- TypeScript
- Vite
- styled-components
- framer-motion
- recharts
- react-router-dom

### Backend
- FastAPI
- TensorFlow/Keras (LSTM)
- Pandas, NumPy
- Matplotlib
- scikit-learn

## 지원 암호화폐

- 비트코인 (BTC)
- 이더리움 (ETH)
- 리플 (XRP)
- 에이다 (ADA)
- 폴카닷 (DOT)
- 체인링크 (LINK)
- 라이트코인 (LTC)
- 비트코인 캐시 (BCH)

## 페이지

- `/` - 메인 대시보드 (포트폴리오, Past ROI, 매매 일지)
- `/charts` - 차트 분석
- `/simulator` - 백테스팅 시뮬레이터

## 매매 일지 사용 방법

1. **백엔드 서버가 실행 중인지 확인** (http://localhost:5001)
2. 메인 페이지에서 "매매 일지" 섹션으로 스크롤
3. **새 매매 기록 등록**:
   - 암호화폐 심볼 입력 (예: BTC, ETH, XRP)
   - 매매 타입 선택 (매수/매도)
   - 가격, 수량, 수수료 입력
   - 매매 일시 선택
   - 메모 추가 (선택사항)
4. **통계 확인**:
   - 누적 수익률
   - 총 매수/매도 횟수 및 금액
   - 평균 매수/매도 가격
   - 총 수수료
5. **기록 관리**:
   - 수정: 각 기록의 "수정" 버튼 클릭
   - 삭제: 각 기록의 "삭제" 버튼 클릭

## API 엔드포인트

### 매매 일지
- `POST /api/trades` - 매매 기록 생성
- `GET /api/trades` - 모든 매매 기록 조회 (필터링 가능)
- `GET /api/trades/{id}` - 특정 매매 기록 조회
- `PUT /api/trades/{id}` - 매매 기록 수정
- `DELETE /api/trades/{id}` - 매매 기록 삭제
- `GET /api/trades/statistics/summary` - 통계 조회

### 백테스팅
- `POST /api/backtest` - 백테스팅 실행
- `GET /api/markets` - 마켓 목록 조회

## 개발

### 프론트엔드 개발
```bash
cd front
npm run dev
```

### 백엔드 개발
```bash
cd backend
python app_fastapi.py
```

## 라이센스

MIT License
